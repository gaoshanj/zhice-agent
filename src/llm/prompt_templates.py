"""LLM Prompt 模板 — 销售策略报告各节生成

每个函数返回一个 messages 列表，直接传给 chat_completion()。
"""

from __future__ import annotations

SYSTEM_PROMPT = (
    "你是一位资深的 To B 企业培训产品销售经理，"
    "精通微软技术培训、AWS 培训、云解决方案销售（MSP）、"
    "安全服务（安服）和管理会计（MA）等领域的销售策略。"
    "你的报告必须：严格基于事实、逻辑清晰、可落地执行、"
    "且严格贴合客户背景与拜访目的。"
    "输出使用中文，语言专业但不晦涩，适合销售人员在客户现场使用。"
    "严禁编造不存在的信息，没有数据就写「暂无相关信息」。"
)

# ── 第 1 节：客户 360° 快照 ──────────────────────────────────

SECTION1_SNAPSHOT = """\
你正在为一次客户拜访准备背景调研报告。

## 客户信息
- 公司名称：{company}
- 拜访对象：{visit_target}
- 已知信息：{known_info}
- 拜访目的：{visit_purpose}
- 侧重方向：{focus_areas}

## 内部知识库信息（重要：以下内容来自企业内部系统，必须优先使用）
下方「内部知识库参考内容」包含了该客户在内部系统中的登记信息，包括行业、客户层级、培训部门、需求详情、商机状态、联系人等。

## 任务
生成「客户 360° 快照」（400 字以内），**必须严格基于上方内部知识库参考内容**，按以下结构组织：

1. **公司概况**：
   - 行业（内部知识库中有则列出）
   - 客户层级（内部知识库中有则列出，如：KA / 战略客户 / 成长潜力等）
   - 培训部门（内部知识库中有则列出，如：IT部门、人力资源部等）
   - 其他关键信息（内部知识库中的已知信息，如客户需求、商机来源等）
   - 注意：如果内部知识库中没有某项信息，明确写「暂无相关信息」，**不可推断或编造**

2. **培训相关信号**：
   - 从内部知识库中提取所有与培训相关的线索（如：已有培训课程、需求触发事件、最近一次培训内容、意向培训内容等）
   - 如果知识库中无培训相关信息，写「暂无培训相关记录」

3. **近期动态**：
   - 基于内部知识库中的「需求触发事件」「下一步关键动作」「预计成交周期」等信息，总结客户近期动态
   - 无相关信息则写「暂无近期动态记录」

4. **拜访切入点**：
   - 结合拜访目的和内部知识库中的客户情况，给出 1-2 个具体的拜访开场角度
   - 例如：如果知识库显示客户刚完成一次培训且满意度高，可以从"了解上次培训反馈"切入

## 输出要求
- 分点陈述，每点不超过 2 行
- 用 **加粗** 标注关键信息（如行业、客户层级、具体培训内容）
- **严禁捏造任何信息**，没有的数据写「暂无相关信息」
- 输出纯文本（将嵌入飞书 Markdown 卡片）
"""


# ── 第 2 节：培训商机深度扫描 ────────────────────────────────

SECTION2_OPPORTUNITY = """\
你正在评估一家客户在 IT 培训领域的商机。

## 客户信息
- 公司名称：{company}
- 拜访对象：{visit_target}
- 拜访目的：{visit_purpose}
- 侧重方向：{focus_areas}
- 客户快照（参考）：{snapshot}

## 内部知识库信息（重要：以下内容来自企业内部系统，必须优先使用）
下方「内部知识库参考内容」包含了该客户的商机登记信息。

## 任务
生成「培训商机深度扫描」（500 字以内），**必须严格基于上方内部知识库参考内容**：

1. **基于知识库的事实性商机描述**：
   - 从知识库中找出所有与培训商机相关的字段（如：客户需求、意向培训内容、需求紧急程度、预计成交周期、交付风险点等）
   - 用原话或概括的方式呈现，不要添加知识库中没有的推测

2. **商机评估**：
   - 如果知识库中有明确的意向培训内容：用 「🎯 高潜力」标注，并说明具体方向
   - 如果知识库中有需求但方向不明确：用 「🟡 中等潜力」标注
   - 如果知识库中无任何商机信息：用 「⚪ 暂无商机数据」标注，并写「当前知识库中暂无该客户的详细商机记录，建议本次拜访重点了解培训需求」

3. **后续跟进建议**（仅基于已有信息）：
   - 知识库中如有「下一步关键动作」，直接引用并补充执行细节
   - 知识库中如有「预计成交周期」，说明当前处于哪个阶段
   - 没有相关信息则跳过，不写空洞建议

## 输出要求
- 严禁编造任何信息，知识库中没有的商机不要"假设"存在
- 每个标注的潜力等级必须有知识库原文支撑
- 输出纯文本
"""


# ── 第 3 节：交叉销售机会挖掘 ────────────────────────────────

SECTION3_CROSS_SELL = """\
交叉销售机会挖掘功能暂未启用。

当前内部知识库暂未接入相关产品体系数据，暂无法提供交叉销售分析。

待后续接入产品线数据后，本模块将分析：培训+咨询捆绑、多部门渗透路径、培训体系化升级、生态伙伴机会等方向。
"""


# ── 第 4 节：销售策略建议 ────────────────────────────────────

SECTION4_STRATEGY = """\
你正在制定本次拜访的销售策略。

## 客户信息
- 公司名称：{company}
- 拜访对象：{visit_target}
- 拜访目的：{visit_purpose}
- 侧重方向：{focus_areas}
- 特别要求：{special_req}

## 参考信息
- 客户快照：{snapshot}
- 商机扫描：{opportunity_scan}

## 内部知识库信息
下方「内部知识库参考内容」可能包含该客户的历史记录，可作为策略参考。

## 任务
生成「销售策略建议」（400 字以内），**必须基于事实、落地可操作**：

1. **拜访目标设定**：
   - 设定 1-2 个可量化、可达成的具体目标
   - 例如："获取客户 IT 部门 20 人的培训需求清单"，而不是"建立良好关系"
   - 如果客户历史记录中有过往培训合作，目标可以是"确认续签意向"或"了解上次培训满意度"

2. **关键信息收集清单**：
   - 列出拜访中需要确认的 3-5 个具体问题
   - 每个问题都要直接服务于后续方案设计
   - 例如：「现有培训预算是按项目还是按年度？」「上次培训后员工考核通过率如何？」

3. **竞争分析**：
   - 仅基于已知信息分析，不要猜测竞争对手
   - 重点放在：我们的差异化优势是什么？如何体现？
   - 例如：如果有培训落地记录，强调"我们有该客户的成功交付经验"

4. **成交路径规划**（具体、可执行）：
   - 从本次拜访到成交的 3-5 个具体步骤
   - 每一步都要有明确的交付物和责任人
   - **示例**：
     - 第1步：本次拜访确认需求 → 输出《需求确认单》
     - 第2步：3个工作日内提供定制化方案（含讲师简历+课程大纲）
     - 第3步：安排 POC 试听课（1-2小时，低成本切入）
     - 第4步：根据 POC 反馈调整方案，提交正式报价
     - 第5步：推动合同签署，确定首批开课时间

## 输出要求
- 用编号列表组织内容
- 关键信息收集清单用 「❓」标注
- 每一步策略都要具体到"做什么、怎么做、何时做"
- 避免空话套话（如"加强沟通""提升服务"），必须可执行
- 输出纯文本
"""


# ── 第 5 节：推荐销售话术 ────────────────────────────────────

SECTION5_TALK_SCRIPT = """\
你正在为销售人员准备面对客户时的具体话术。

## 客户信息
- 公司名称：{company}
- 拜访对象：{visit_target}
- 拜访目的：{visit_purpose}
- 侧重方向：{focus_areas}

## 参考信息
- 客户快照：{snapshot}
- 销售策略：{strategy}

## 任务
生成「推荐销售话术」（400 字以内），**话术必须真实可用、自然不推销**：

### 开场白话术（30 秒）
- 结合客户快照中的已知信息设计自然开场
- 如果客户有历史培训记录：可以从"上次培训反馈"或"听说贵司最近在推 XX 项目"切入
- 如果首次接触：可以从行业痛点或共同认识的人切入
- 示例：「张总您好，了解到贵司最近在推进数据治理项目，我们刚帮 XX 公司做了类似的 Power BI 培训，想跟您聊聊这方面的经验。」

### 痛点挖掘提问（3-5 个）
- 每个问题都要有明确的提问目的
- 问题要具体到客户的业务场景，不要泛泛而谈
- 示例：
  - 「贵司目前新系统上线后，员工的培训周期一般是多长？」（了解培训效率痛点）
  - 「上次培训后，部门内部有没有做过效果评估？考核通过率怎么样？」（了解培训效果）

### 方案价值陈述（1-2 句话）
- 用客户能听懂的语言，一句话说明我们能解决什么问题
- 示例：「我们可以为贵司提供一套从基础到进阶的 Power BI 培训方案，2 周内让核心团队具备独立开发报表的能力。」

### 下一步行动引导
- 给出具体的下一步建议，不要让客户"回去考虑"
- 示例：「如果您方便，我可以安排讲师下周过来做一次 2 小时的免费 POC 试讲，让团队先体验一下课程风格？」

## 输出要求
- 话术用 「> 引用格式」标注
- 附上「使用提示」（什么场景下用，注意什么）
- 话术要自然、口语化，像真人在说话，不要书面语堆砌
- 输出纯文本
"""


# ── 第 6 节：行动建议 ────────────────────────────────────────

SECTION6_ACTION_PLAN = """\
你正在为销售人员制定拜访后的具体行动计划。

## 客户信息
- 公司名称：{company}
- 拜访对象：{visit_target}
- 拜访目的：{visit_purpose}

## 参考信息
- 销售策略：{strategy}

## 任务
生成「行动建议」（300 字以内），**每个建议都要具体到动作、时间、责任人**：

1. **拜访后 24 小时内**：
   - 必须完成的具体动作（不是"发封邮件"，而是"发送《会议纪要》并抄送 xxx"）
   - 示例：发送拜访纪要邮件，重点确认今天谈到的 3 个需求点；在 CRM 中更新客户拜访记录

2. **一周内**：
   - 需要推进的具体事项，明确交付物和截止时间
   - 示例：周三前完成定制化培训方案初稿；周五前联系讲师确认档期

3. **一个月内**：
   - 如果顺利，应该达成的具体里程碑
   - 示例：完成 POC 试讲并获取客户反馈；提交正式报价并进入合同谈判

4. **风险预警**：
   - 本次拜访可能遇到的具体异议及应对话术
   - 示例：客户说"预算还没批" → 回应"理解，我们先安排一次免费 POC，预算批下来后立即启动"

## 输出要求
- 用 「⏰」标注时间节点
- 风险预警用 「⚠️」标注
- 每个建议都具体到"谁、在什么时间、做什么"
- 避免模糊表述（如"保持联系""持续跟进"）
- 输出纯文本
"""


# ── 构建 messages ─────────────────────────────────────────────

def build_section_messages(section_num: int, **kwargs) -> list[dict]:
    """根据章节号，构建传给 LLM 的 messages 列表。

    Args:
        section_num: 1-6，对应 6 个章节
        **kwargs: 传递给模板的变量（company, visit_target 等）
                     可选传入 rag_context（RAG 检索结果文本）

    Returns:
        list[dict]: [{"role": "system", ...}, {"role": "user", ...}]
    """
    template_map = {
        1: SECTION1_SNAPSHOT,
        2: SECTION2_OPPORTUNITY,
        3: SECTION3_CROSS_SELL,
        4: SECTION4_STRATEGY,
        5: SECTION5_TALK_SCRIPT,
        6: SECTION6_ACTION_PLAN,
    }
    template = template_map.get(section_num, SECTION1_SNAPSHOT)
    user_content = template.format(**kwargs)

    # 追加 RAG 检索结果（如有）
    rag_context = kwargs.get("rag_context", "")
    if rag_context:
        user_content += "\n\n" + rag_context

    return [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": user_content},
    ]


def build_summary_messages(report_data: dict) -> list[dict]:
    """（预留）生成报告摘要的 messages"""
    return [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": f"请用 100 字以内总结以下报告的核心建议：\n\n{report_data}"},
    ]
